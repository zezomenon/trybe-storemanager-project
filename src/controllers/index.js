const { 
  createProducts,
  getProducts,
  getProductsById,
  updateProductsById,
} = require('./productsController');

module.exports = {
  createProducts,
  getProducts,
  getProductsById,
  updateProductsById,
};
